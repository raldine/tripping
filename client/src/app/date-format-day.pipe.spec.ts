import { DateFormatDayPipe } from './date-format-day.pipe';

describe('DateFormatDayPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatDayPipe();
    expect(pipe).toBeTruthy();
  });
});
